{% extends 'base.html.twig' %}

{% block body %} 
    <head>
        <META HTTP-EQUIV="CACHE-CONTROL" CONTENT="NO-CACHE" /> 
    <meta charset="utf-8">
    <title>{% block title %}Profile{% endblock %}</title>
        {% block stylesheets %}
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <!-- Bootstrap -->
     <link rel="stylesheet" href="{{ asset('css/topmenu.css') }}" />
    <link href="{{ asset('css/bootstrap.css')}}" rel="stylesheet"/>
    <link rel="stylesheet" href="{{ asset('css/font-awesome.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css') }}">
    
 
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
     {% endblock %}
  </head>
  
  
<body> 
    

    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4 col-sm-6  col-sm-offset-3">
                <div class="reg-header">
                     {%if data.id !=app.request.cookies.get('id')%}
                        {%if cer.cereri is defined%}    
                            {%if cer.cereri !=null%}
                             <button type="button" class="btn">Cerereaa a fost trimisa</button>
                             {%else%}
                                 {%if cer.friends is defined%}
                                     {%if cer.friends !=null%}
                                        <input type="button" value="Sunteti Prieteni" class="btn ">
                                     {%else%}
                                      <div id="cererep">
                                          <form id="form" name="form">
                                            <input id="submit" type="button" value="Adauga" class="btn ">
                                         </form>
                                      </div>
                                     {%endif%}
                                 {%endif%}
                              {%endif%}
                        {%endif%}
                        {%endif%}
                    </div>
                </div>
             </div>
        </div>
 <!-- script valic -->
     
 <script>
$(document).ready(function() {
$("#submit").click(function() {
// Returns successful data submission message when the entered information is stored in database.
$.post("{{url('adaugaprieten',{'id':data.id})}}", {
id:{{data.id}},
}, function(data) {
});
$('#cererep').html("<button type='button' class='btn'>Cerereaa a fost trimisa</button>");
});
});
</script>
<!-- script valic cereri de prietenie   asddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd-->
    <div class="container container1">
    <div class="row">
        <div class="col-md-3 col-lg-3">
            <div class="hovereffect">
            <img class="img-responsive" src="{{asset(data.image)}}"alt="{{data.username}}"  height="100" width="300" >
                <div class="overlay">
                    <h2>{{data.nume}}</h2>
                    <p class="icon-links">
                        <a href="#">
                            <span class="fa fa-camera"></span>
                        </a>
                            <a href="#"> <span class="fa fa-thumbs-o-down"></span>
                        </a>
                        <a href="#">
                            <span class="fa fa-thumbs-o-up"></span></a>
                    </p>
                </div>
        </div>
        </div>
        <div class="col-md-3 hidden-xs">
            
        </div>
        <div class="col-md-3  hidden-xs">
          
            
        </div>
         <div class="col-md-2 hidden-xs">
           
        </div>
    </div>
</div>
    <!--
 <div class="container profil-container ">
     <div class="row ">
         <div class="col-md-12 ">
             <div class="row">
                 <div class="col-md-6">
                     <a href="#"><img  src="{{asset(data.image)}}"alt="{{data.username}}" height="320" width="200" class="img-thumbnail"> </a>
                 </div>
                 <div class="col-md-6">
                     <p>  
                         <span>{{data.nume}}{{data.prenume}}</span>
                         <span>{{  data.An | date("d/m/Y") }}</span>
                     </p>
                 </div>
             </div>
         </div>
                     <div class="row ">
        <div class="col-md-12 ">
            <div class="row">
                <div class="col-md-6">   
                    
                    <div class="col-md-6">
                        
                      {%if data.id !=app.request.cookies.get('id')%}
                        {%if cer.cereri is defined%}    
                            {%if cer.cereri !=null%}
                             <button type="button" class="btn btn-success">Cerereaa a fost trimisa</button>
                             {%else%}
                              <a type="button" class="btn btn-danger"  href="{{url('adaugaprieten',{'id': data.id})}}">Adauga</a>
                               {%endif%}
                        {%endif%}
                            <div class="grid_9">
                                <nav class="mid_menu">
                                    <ul>
                                        <li><a href="#"> Message</a><span class="top_haver"></span></li>
                                        <li ><a href="{{url('friends',{'id': data.id})}}"> Frends</a><span class="top_haver"></span></li>
                                        <li><a href="#"> Grups</a><span class="top_haver"></span></li>
                                        <li><a href="exit"> Music</a><span class="top_haver"></span></li> 
                                     </ul>
                                </nav>
                            </div>
                        {%endif%}
                    </div>
                 </div>
                    </div>
           </div>
      </div>
    </div>                     
 </div>
   --->      
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                  {% if cer.cererip is defined %}
                    {% for cer in cer.cererip %}
                        <div id="{{cer.id}}">
                        <div style="background-color: blueviolet; width: 200px; float:left; margin-right: 20px; ">
                            <h3>{{cer.nume}} {{cer.prenume}}</h3>
                            <form id="form" name="form">
                                <input id="accept{{cer.id}}" type="button" value="Aceapta" class="btn btn-success"/>
                                <a href="#" type="button" class="btn btn-Danger">Refuza</a>
                            <form>
                        </div>
                        </div>
<script>
$(document).ready(function() {
$("#accept{{cer.id}}").click(function() {
// Returns successful data submission message when the entered information is stored in database.
$.post("{{url('accept',{'id':cer.id})}}", {
id:{{cer.id}},
}, function(data) {
});
$('#{{cer.id}}').html("<button type='button' class='btn' style='width: 200px; float:left; margin-right: 20px;'>Acceptat</button>");
});
});
</script>
                    {% endfor %}
                   {% endif %}
                </div>
            </div>
     </div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.js"></script>     
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="js/bootstrap.js"></script>
</body>
{%endblock%}