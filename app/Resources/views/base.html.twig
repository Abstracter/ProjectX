<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Holla!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
            <link rel="stylesheet" href="{{ asset('css/css.css') }}" />
            <link href="{{ asset('css/bootstrap.css')}}" rel="stylesheet"/>
            <link rel="stylesheet" href="{{ asset('css/topmenu.css') }}" />
            <link rel="stylesheet" href=css/font-awesome.css'>

              <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
        {% endblock %}
       
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
       
    </head>
    {% block header %}
        <!--
  <header>
        <div clss="container_12">
          <div class="grid_3">
           
          </div>
           <div class="grid_9">
               <nav class="top_menu">
                   <ul>
                     <li><a href="{{url('profile', {'id': app.request.cookies.get('id')})}}"> Profil</a><span class="top_haver"></span></li>
                       <li ><a href="{{url('chat_homepage')}}"> Message</a><span class="top_haver"></span></li>
                         <li ><a href="#">Grup</a><span class="top_haver"></span></li>
                           <li ><a href=""> Chat</a><span class="top_haver"></span></li>
                       <li><a href="{{url('photo',{'id': app.request.cookies.get('id')})}}"> Photo</a><span class="top_haver"></span></li>
                       <li><a href="{{url('friends',{'id': app.request.cookies.get('id')})}}">Friends</a><span class="top_haver"></span></li>
                        <ul style="float:right;list-style-type:none;">
    <li><a class="exit" href="{{ url('exit') }}">Exit</a></li>
                    </ul>
               </nav>
           </div>
         </div>
    </header>
               -->
               
<div class="navbar navbar-inverse navbar-static-top" >
    <div class="container" >
        <div class="navbar-header "id="topmenu" >
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#re-menu">
               <span class="sr-only">Open menu</span>
               <span class="icon-bar"></span>
               <span class="icon-bar"></span>
               <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{url('profile', {'id': app.request.cookies.get('id')})}}">CONIO</a>
        </div>
        <div class="collapse navbar-collapse" id="rp-menu">
            <ul class="nav navbar-nav">
                 <li>
                     <a href="{{url('profile', {'id': app.request.cookies.get('id')})}}"> Profil</a>
                 </li>
                   <li role="presentation">
                       <a href="{{url('chat_homepage')}}" > Message</a>
                   </li>
                     <li >
                         <a href="#">Grup</a>
                     </li>
                       <li >
                           <a href=""> Chat</a>
                       </li>
                             <li>
                                 <a href="{{url('photo',{'id': app.request.cookies.get('id')})}}"> Photo</a>
                             </li>
                                 <li>
                                     <a href="{{url('friends',{'id': app.request.cookies.get('id')})}}">Friends</a>
                                 </li>



             </ul>
                 <ul class="nav navbar-nav navbar-right">
                     <li><a href="{{ url('exit') }}">Log out</a></li>
                 </ul>
        </div>
     </div>
 </div>
                 <div class="moda fade"id="modal-1">
                     <div class="modal-dialog modal-lg">
                         <div clas="modal-content">
                             <div class="modal-header">
                                 <div class="modal-body">
                                     <div class="modal-footer">

                                     </div>
                                 </div>
                             </div>
                         </div>
                     </div>
                 </div>

                    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    
    {%endblock%}
    
        {% block body %}{% endblock %}
        
    
</html>
